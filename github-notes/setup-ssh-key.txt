
$ ssh-keygen -t rsa -b 4096 -C https://github.com/moonapnd@gmail.com
Generating public/private rsa key pair.
...

$ eval "$(ssh-agent -s)"
Agent pid 3755

$ ssh-add ~/.ssh/id_rsa
Identity added: ... 

$ sudo apt-get install xclip

$ xclip -sel clip < ~/.ssh/id_rsa.pub


go to your GitHub account > settings > SSH and GPG keys > new SSH key > (enter the title and past the key)
